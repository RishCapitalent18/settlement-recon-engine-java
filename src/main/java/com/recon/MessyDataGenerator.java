package com.recon;

/**
 * The messy internal ledger and custodian feed are synthesized by
 * tools/generate_data.py and committed under data/ so results are
 * reproducible. This class is intentionally left as a placeholder; the
 * engine reads the committed CSVs rather than generating data at runtime.
 */
final class MessyDataGenerator { private MessyDataGenerator() { } }
